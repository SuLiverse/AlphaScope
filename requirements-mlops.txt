# 研策中枢 AlphaScope MLOps 可选依赖
# Install: pip install -r requirements-mlops.txt
#
# 所有包缺失时对应能力优雅降级, 不影响核心功能。

cryptography>=50.0  # pin so isolated mlops audit does not resolve 49 (PYSEC-2026-3552)
mlflow>=2.0  # 实验管理/模型注册/LLM trace
optuna>=3.0  # 超参优化
langgraph>=0.2  # 多 Agent 工作流编排
evidently>=0.4  # 数据漂移与质量监控
prefect>=3.0  # Python 工作流 (调度/重试/缓存)
opentelemetry-sdk>=1.20  # 分布式追踪 (trace/metrics/logs 标准)

# ragas intentionally stays out of the default MLOps bundle while 0.4.3 is the
# latest release and carries PYSEC-2026-3046 / CVE-2026-6587 (no fixed release).
# Install it explicitly only in isolated evaluation environments:
#   pip install ragas>=0.1
