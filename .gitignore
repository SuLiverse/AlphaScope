# Environment files
.env
.env.local
.env.*.local
.env.bak
*.db.bak

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
build/
dist/
installer/installer-output/

# Release seed DB is versioned so clean CI builds keep the zero-key demo usable.
seed/*
!seed/ai_finance.db

# Virtual envs
venvs/
venv/
env/
ENV/
.venv/

# Data caches
data/cache/
data/db/
data/uploads/
data/datalake/
data/logs/
data/raw/
data/runtime/
cache/
uploads/
.first_run_complete
*.parquet.tmp

# Reports / logs
data/reports/*.log
data/reports/streamlit-*.err
data/reports/archive/
data/reports/roundtables/
data/reports/chat_history/
reports/archive/
*.log

# External study repos (separate git projects, not part of this repo)
# apps/  # Now used for React frontend - only ignore node_modules
apps/web/node_modules/
apps/web/.next/
# 后端 lifespan 会写入本机 token，勿提交
apps/web/public/runtime-config.js
apps/web/dist/runtime-config.js

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Streamlit
.streamlit/secrets.toml

# Local screenshots / feedback dropped by the user (kept off VCS)
反馈/
feedback/

# Claude Code local settings (per-machine permissions)
.claude/settings.local.json

# Notebooks checkpoints
.ipynb_checkpoints/

# Stray test artifacts
analysis_out.json

# filter-branch/filter-repo 临时产物
.git-rewrite/
refs/original/
