# Implementation Plans

Generated by the improve skill on 2026-07-27, base commit `98478de`；2026-08-01 增补
021-025（第二轮审查，HEAD `06182ca`）。Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

范围说明：本次立项覆盖 advisor 审计的 Tier 1 全部发现 + Tier 2 的 S 级速赢（用户选定"连速赢一起全包"）。M/L 级技术债与方向性项目见文末 considered 区，未立项。

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | 清除已暴露密钥实物并完成轮换 | P1 | S | — | DONE（已合并 main @06182ca 并强推；本地+远端历史已 scrub，v1.9.39 同步更新；9 个 provider key 已吊销换发；残留可选项：GitHub PR refs 需 support 工单清除，仅存作废旧 key） |
| 002 | DuckDB 数据湖守卫加固（文件读/SSRF 绕过） | P1 | S | — | DONE（分支 `advisor/002-datalake-guard-hardening` @8224245，黑名单+字面量表名+扩展锁定+错误脱敏+限流，全量 2022 过） |
| 003 | 回测引擎信号对齐（未来函数）+ 指标准确性四件套 | P1 | M | 025 先行 | DONE（分支 `advisor/003-backtest-signal-alignment` @6a71a46，先红后绿 7 commit + walk_forward 年化口径 follow-up，全量 2013 过） |
| 004 | 定投模拟数学三处修复 + 遗留 DCA 假公式 | P1 | M | 025 先行 | DONE（分支 `advisor/004-dca-math-fixes` @4b12a72，XIRR 二分 + 禁未来净值 + 单位口径回撤 + legacy 公式重写 + golden 期望值同步更新，全量 2013 过） |
| 005 | Auto 模式预筛失败降级，不升级 DEEP | P2 | S | — | DONE（分支 `advisor/005-prescreen-failure-no-escalation` @72ffb43，预筛失败降级返回，confidence=50 仍升级，全量 2009 过） |
| 006 | 回测运行详情缓存淘汰方向 | P2 | S | 003 先行 | DONE（分支 `advisor/006-quant-run-cache-eviction` @87b23fa，`_evict_local_run_details()` helper 同源，全量 2015 过） |
| 007 | async 端点同步阻塞调用卸载 | P2 | S | — | DONE（分支 `advisor/007-async-endpoint-offload` @97acef4，4 端点 7 调用点 to_thread 卸载，全量 2008 过） |
| 008 | 通知渠道 SSRF 加固（feishu/SMTP） | P2 | S | — | DONE（分支 `advisor/008-notifier-ssrf-hardening` @9f28986，主机名边界匹配+url_guard IP 校验+SMTP 私网闸门+逃生门，全量 2017 过） |
| 009 | Docker token 卫生（access log + 静态分发模型） | P1 | M | 建议 002 先行 | DONE（分支 `advisor/009-docker-token-hygiene` @b49fc2c，3 commits：no-access-log + 共享卷 token 置空 + 前端引导页/sessionStorage/活绑定，全量 2010 过；**前端 vitest/eslint 未跑**——node_modules 缺失，人工清单项） |
| 010 | 聊天端点 SQLite 连接泄漏（共享 ConversationStore） | P2 | S | — | DONE（分支 `advisor/010-chat-store-shared` @6b79fff，双重检查锁懒加载共享 store，全量 2010 过） |
| 011 | 自定义 key LLM 客户端缓存生命周期 | P2 | M | — | 已核验 HEAD 修复（provider_gateway.py:693-694 + 712-714 + settings_store.py:808），无需分支 |
| 012 | experts.yaml / prompt 文件 mtime 记忆化 | P3 | S | — | DONE（分支 `advisor/012-experts-config-memoization` @78f8408，mtime 键缓存 + reload 钩子 + 4 用例，全量 2011 过） |
| 013 | 组合写失败传播 + 基本面错误不缓存 | P2 | S | — | DONE（分支 `advisor/013-store-honesty` @d3de644，失败显式传播 + PORTFOLIO_SAVE_FAILED + 错误结果不入缓存，全量 2017 过） |
| 014 | 任务 SSE 终态结束 + cancel 标记泄漏 | P3 | S | — | DONE（分支 `advisor/014-task-sse-completion` @78841d7，`_collect_task_events` helper + 终态 return + 标记后置，全量 2014 过） |
| 015 | 拆除 import 时全进程 warnings 静默 | P3 | S | — | DONE（分支 `advisor/015-scope-warning-filters` @9b2dc9f，4 模块删除模块级静默，警告面 13 条全第三方无需收敛，全量 2011 过） |
| 016 | 测试期依赖声明 + 本地/CI 口径对齐 | P3 | S | — | DONE（分支 `advisor/016-dev-test-dependencies` @83f4d88，httpx/fastapi/pytest-cov 双清单 + Makefile/CONTRIBUTING 对齐 + gitignore，skip 数不变） |
| 017 | 依赖钉版升级（删 aiohttp + 三包 CVE） | P2 | S | — | DONE（分支 `advisor/017-dependency-pin-updates` @77bddc4，删 aiohttp 零导入、requests 2.33.0 / curl_cffi 0.15.0 / python-dotenv 1.2.2，pip-audit 35→0 advisory；**重装环境 + 联网烟测为人工项**） |
| 018 | 聚合 K 线对非法 OHLC 免疫 | P3 | S | — | DONE（分支 `advisor/018-kline-aggregation-guard` @8e7b6c5，先红后绿（0.0==9 证据）+ valid 过滤 + 边界用例，全量 2011 过） |
| 019 | Prefect 测试不再拉起真实服务器 | P3 | S | — | DONE（分支 `advisor/019-prefect-test-isolation` @1358f68，monkeypatch `_prefect_flow/_prefect_task` 桩，temporary server 噪音 1→0，全量 2007 过） |
| 020 | 文档/清单版本漂移清理 | P3 | S | — | DONE（分支 `advisor/020-docs-version-drift` @346c865，去硬编码版本 + 13 条路径参数名对齐 + README 2000+ 口径，全量 2007 过） |

## 第二轮（2026-08-01，HEAD `06182ca`）

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 021 | 复活自选股监控告警（get_price_range 误用 + 形状错配） | P1 | S | — | DONE（分支 `advisor/021-watchlist-alerts-fix` @1cbb4f5，全量 2006 过） |
| 022 | MCP search_evidence 接入真实检索入口 | P1 | S | — | DONE（分支 `advisor/022-mcp-search-evidence-fix` @6012edf，全量 2003 过） |
| 023 | 回测全胜 profit_factor=inf → 500 防护 | P1 | S | — | DONE（分支 `advisor/023-profit-factor-inf-guard` @a657857，全量 2003 过） |
| 024 | 混合检索排序反向 + BM25 腿修复 | P1 | M | — | DONE（分支 `advisor/024-hybrid-retriever-ranking-fix` @e78ff99，先红后绿，全量 2004 过） |
| 025 | DCA/回测精确值测试网（**003/004 前置**） | P1 | M | 023 | DONE（分支 `advisor/025-exact-value-test-net` @c8aab49，内含 023 合并，全量 2007 过） |
| 026 | walk_forward 年化口径随 003 对齐（003 follow-up） | P1 | S | 003 | DONE（随 003 分支 @6a71a46 一起合并，无独立分支） |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- **001 先行**：密钥实物暴露期间，其他工作都在扩大已泄露凭据的存续时间；002/009 压缩的是同一 token 的爆炸半径，排在 001 之后。
- **003 是一次性改变全部回测数字的修复**，应先于任何其他动回测数字的工作落地；006 与其同文件（`quant_core.py`）不同行段，无硬冲突但建议 003 先合并以保持 diff 干净。
- **009 建议排在 002 之后**：002 先堵"拿到 token 即可任意文件读/SSRF"的最重后果，009 再修 token 分发模型本身。
- **013/015/010/011 会互相印证**：015 拆除 warnings 静默后，010/011 修的未关闭资源问题未来可被 ResourceWarning 提前暴露——无执行顺序要求。
- **025 必须先于 003/004**：003/004 会改变回测/DCA 的全部数字，025 先用手算精确值钉死当前行为作为回归网；003/004 合并时须同步更新 025 的期望值（025 测试注释内已写指引）。**025 已包含 023**（其 golden 测试的 profit_factor 断言对齐 023 的 None 语义），合并 025 前先合 023 或直接合 025 分支（内含 023 commit）。003/004 分支均从 025 分出（含其全部回归网）；003 落地时更新了 025 引擎 golden 的 trading_days 断言保持 12（003 保留 trading_days 交易日口径），004 落地时按闭式手算重推了 TestDCAGoldenValues 全部期望值（新口径 XIRR）。
- **003 合并顺序**：003 分支含 walk_forward 年化口径 follow-up（计划外发现的同一口径遗漏，单行粒度合并或随分支整体合并均可）。
- **004 与 003 的合并交互**：二者均改 `backend/api/funds.py`（003 改 get_fund_metrics 传 dates，004 改 _legacy_dca_result）——不同函数，git 合并无冲突；003 改 `backend/quant/metrics.py` 而 004 未触碰，均安全。
- **024 建议先于 022 评审、可先于 022 合并**：022 的 MCP 工具在 024 修复前返回的结果排序是错的；二者无代码冲突，但合并顺序 024→022 体验更一致。
- 其余计划相互独立，可并行分派给不同执行者。

## Direction findings（已记录，未立项——产品方向决策）

1. **SSE 真流式**：`/api/chat/stream` 目前是"算完再按 20 字/20ms 重放"的假流式（`backend/api/main.py:568-618`）。SSE 管道、前端状态、契约测试都已就绪，接上 provider 层增量输出是感知性能收益最大的一步（M，涉及 orchestrator/provider 接口演进）。
2. **staged 能力生死判决**：`backend/workflow_orchestration.py`（Prefect 封装）与 Tool Router 均为"测试全绿、产品未接"状态（前者已核实除测试外零调用方）。要么接进编排/追踪，要么删除——挂着不动让测试替死代码伪装成活代码。
3. **新闻摄取收尾到 Provider 注册表**：`news_data.py` 遗留抓取与 `providers/` 双管道并存、字段形状已分叉（docstring 自承 v0.11 过渡未收尾）。合并后数据源健康/降级机制才能覆盖全部入口（L）。
4. **组件测试安全网先于继续拆组件**：React 组件零测试且 vitest 为 node 环境（无 jsdom/Testing Library）。typed-props 重构战役的剩余 4 个大组件（NewsAggregator/FundDcaLab/MultimodalChart/ReportGenerator）风险递增，建议先补 jsdom+RTL 种子测试再继续（M）。

## Findings considered and rejected（已审查，不立项，避免重复审计）

- **quant_core.py vs quant/local_runner.py 双份实现漂移**（DEBT-01，M）：真实但属合并重构，需保持 local_runner 无 FastAPI 依赖的边界；待 003/006 落地后单独立项。
- **sys.path hack / 双模块身份**（DEBT-03，M）：机制确认、当前危害有限（无可达双重单例）；修复牵涉 Streamlit 调试台启动方式，暂缓。
- **死 API 面 portfolio_new + fund-portfolio CRUD**（DEBT-05，S）：删除前需确认无脚本/第三方调用方；列入下一批。
- **新闻 4 源串行抓取 + 逐行 commit**（PERF-05，M）：并行化改变失败语义，需设计；列入下一批。
- **前端 AbortController 全链路**（PERF-07，M）：需穿 `fetchApi` 与各 effect，M 级；列入下一批。
- **SSE 任务流以外的前端重渲染优化**（PERF-07 前半，S）：NewsAggregator memoization，影响低，随组件拆分顺带处理。
- **裸 `.conn` 绕过 Database 锁 + 审计日志静默吞错**（CORR-13，置信 MED）：sqlite 序列化兜住了大部分风险，演示出的失败模式是静默吞错而非数据损坏；审计日志 `except: pass` 应修，列入下一批。
- **新闻链接解析重定向链盲 SSRF**（SEC-06，M）：仅盲 GET、利用价值中低；修法是逐跳校验或迁移到 pinned-DNS httpx，列入下一批安全专项。
- **两条 OpenAI 客户端路径未走 pinned-DNS transport**（SEC-07，置信 MED）：利用需攻击者控制用户自定义 provider 的 DNS；修法同 SEC-06 迁移，合并处理。
- **workflow_orchestration.py 无生产调用方**：见其上 Direction #2；019 已消除其测试副作用。
- **README "100+ APIs"**：实测 200 端点/37 路由文件，声明保守成立，无需处理。
- **requirements 与 pyproject 漂移**：逐钉比对零漂移（已核验），016 的 additions 维持该约定。
- **SQL 注入面**（news_store/price_store/alert_store/conversation_store 的 f-string 查询）：插值均为静态列名/关键字、值全参数化——已核验，无发现。
- **上传/archive 路径穿越、token 生成与比较、subprocess 用法、pickle/eval 反序列化**：均已核验干净。
- **`_archive/` 与 `mcps/` 目录**：在工作区根、不属于发布树，未审计。
- **未审计范围**（下次应覆盖）：`rag/`、`vision/`、`mcp_server.py` 深读；前端 XSS 面；tdx_compiler 公式沙箱（未 fuzz）；Streamlit 调试台逻辑漂移；运行时压测。
