name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install --prefer-binary -r requirements-dev.txt -r requirements-api.txt

    - name: Lint with ruff
      run: |
        ruff check backend/ frontend/ tests/ --output-format=github
        ruff format --check backend/ frontend/ tests/

    - name: Run tests
      env:
        DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        CLAUDE_API_KEY: ${{ secrets.CLAUDE_API_KEY }}
      run: |
        python -m pytest tests/ -v --tb=short -m "not network" --ignore=tests/probes

  dependency-audit:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - name: Audit Python dependencies
      env:
        PYTHONUTF8: "1"
      run: |
        python -m pip install --upgrade pip pip-audit
        python -m pip_audit -r requirements-core.txt -r requirements-api.txt
        python -m pip_audit -r requirements-streamlit.txt
        python -m pip_audit -r requirements-quant.txt
        # Audit mlops with core so cryptography>=50 from core wins over
        # mlflow's isolated resolve (which otherwise settles on 49).
        python -m pip_audit -r requirements-core.txt -r requirements-mlops.txt

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: "20.19.0"
        cache: "npm"
        cache-dependency-path: apps/web/package-lock.json

    - name: Audit web dependencies
      working-directory: apps/web
      run: npm audit --audit-level=moderate

  web-build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: "20.19.0"
        cache: "npm"
        cache-dependency-path: apps/web/package-lock.json

    - name: Install dependencies
      working-directory: apps/web
      run: npm ci

    - name: Test
      working-directory: apps/web
      run: npm test

    - name: Build
      working-directory: apps/web
      run: npm run build

    - name: Lint
      working-directory: apps/web
      run: npm run lint

  docker-build:
    runs-on: ubuntu-latest
    needs: [lint-and-test, dependency-audit, web-build]

    steps:
    - uses: actions/checkout@v4

    - name: Build Docker image
      run: docker build -t alphascope:test .

    - name: Verify Docker image
      run: docker run --rm alphascope:test python -c "import backend; print('OK')"
