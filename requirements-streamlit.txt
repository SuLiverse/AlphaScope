# Optional Streamlit debug console (NOT the product UI).
# Product UI is apps/web (Vite + React 19). Install only if you need:
#   python -m streamlit run frontend/dashboard.py
# or run_dashboard.bat

-r requirements-core.txt
streamlit==1.57.0
plotly==6.7.0
